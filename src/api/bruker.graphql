type Query {
    ping: String
    notifikasjoner: [Notifikasjon!]!
}

union Notifikasjon = Beskjed | Oppgave

type Beskjed {
    merkelapp: String!
    tekst: String!
    lenke: String!
    opprettetTidspunkt: String!
}

type Oppgave {
    merkelapp: String!
}